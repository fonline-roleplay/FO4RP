#ifndef TRAP_H
#define TRAP_H

#define TRAP_HARDNESS 				Val0
#define TRAP_SKILL					Val1
#define TRAP_OWNER					Val2
#define TRAP_CRITICAL_CHANCE		Val3

#define TRAP_MECHANICAL				( 0 )
#define TRAP_MINE					( 1 )
#define TRAP_TRIPWIWE				( 2 )

	#ifndef TRAP_MODULE
		import bool TrapCritRoll( Item& trap, Critter& target ) from "trap";
		import bool TryPlantMine( Critter& cr, Item& trap, uint16 hexX, uint16 hexY ) from "trap";
	#endif // TRAP_MODULE

#endif // TRAP_H