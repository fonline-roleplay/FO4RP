#ifndef CRAFT_H
#define CRAFT_H

	#define CRITTER_UNABLE_TO_CRAFT # (crafter)!valid(crafter) || crafter.IsDead() || crafter.IsKnockout() || HandsTied(crafter)
	
	#define CRAFT_SK_MOD	( 65 )
	#define CRAFT_SK_MUL	( 10 )
	
#endif