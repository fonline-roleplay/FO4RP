#ifndef WAIT_TIME_H
#define WAIT_TIME_H

	#ifndef WAIT_TIME
		import uint GetProtoTime( const ProtoItem& prot, uint8 mode, CritterMutual@ crit ) from "wait_time";
		import uint GetProtoWindupTime( const ProtoItem& prot, uint8 mode, CritterMutual@ crit ) from "wait_time";
		import uint GetSkillTime ( int sk, CritterMutual@ crit ) from "wait_time";
		import uint GetActionTime( int action, CritterMutual@ crit ) from "wait_time";
	#endif //WAIT_TIME

#endif //WAIT_TIME_H