#ifndef APREGEN_H
#define APREGEN_H

	#ifdef __SERVER

		#ifndef APREGEN
			import uint cte_RegenAp(Critter& cr, int identifier, uint& rate) from "apregen";
			import bool SetCritterApRegen(Critter& cr) from "apregen";
		#endif // APREGEN

	#endif // __SERVER

#endif // APREGEN_H
