#ifndef WEATHER_H
#define WEATHER_H

	#define WEATHER_CHANGE_TIME (10)
	#define RAIN_INCREASE_TIME  (1)
	#define WIND_SOUND_TIME		(60)

	#ifndef WEATHER_MODULE
		import void applyWetness( Critter& cr ) from "weather";
		import void RainSound( Map& map, int strength ) from "weather";
	#endif WEATHER_MODULE
	
#endif // WEATHER_H