#ifndef __SUI_EVENT_LISTENERS__
    #define __SUI_EVENT_LISTENERS__

    #include "sui_event_listeners.fosh"

    shared interface EventListenerSUI {
        void OnEvent(EventSUI@ event);
    }

    shared interface LoopListenerSUI {
        void OnLoop();
    }

    #ifdef __CLIENT
        shared interface RenderListenerSUI {
            void OnRender(uint layer);
        }
    #endif

#endif