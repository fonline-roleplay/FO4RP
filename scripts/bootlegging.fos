// Author: Anuri

#include "_macros.fos"

import void ExplodeEx( Map& map, uint16 hexX, uint16 hexY, uint16 effectPid, uint effectRadius, uint damage, uint damageType, uint damageRadius, uint ownerId )  from "explode";
import void InjureCritter( Critter& cr, uint dmg, uint dmgType, uint8 dir, uint attackerId ) from "combat";

#define STATE						Val0 	// Статус 0 - потух, 1 - работает, 2 - сломан
#define MAX_FUEL 					Val1 	// Макс допустимый заряд (верхний передел загрузки топлива) 
#define CURRENT_FUEL				Val2 	// Текущий заряд топлива
#define EVENT 						Val3 	// Для записи тайм-ивента работы аппарата
#define MAX_RAW_MATERIAL			Val4	// Максимальный загруз браги
#define CRURRENT_RAW_MATERIAL		Val5	// Текущий загруз браги
#define CRITICAL_TEMPRATURE			Val6	// Критическая температура, после которой возможна поломка
#define	CURRENT_TEMPRATURE			Val7	// Текущая температура
#define CRITICAL_PRESSURE_LEVEL		Val8	// Критическое давление, после которого возможен взрыв
#define CURRENT_PRESSURE			Val9	// Текущее давление


