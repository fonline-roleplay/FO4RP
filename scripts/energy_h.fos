#ifndef ENERGY_H
#define ENERGY_H

const uint[] energy_cells = { PID_MICRO_FUSION_CELL, PID_SMALL_ENERGY_CELL, PID_CUENCELL_LOW };

	#ifndef ENERGY
		#ifdef __SERVER
			import void InitBatteryRules() from "energy";
			import uint GetEnergyChargeFromBattery( uint16 batteryPid, Item& rechargeableItem ) from "energy";
			import bool ChargeItem( Critter& cr, Item& batteryItem, Item& rechargeableItem ) from "energy";
		#endif // __SERVER
	#endif //ENERGY
#endif //ENERGY_H
