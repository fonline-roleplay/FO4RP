#ifndef BRAHMINS_H
#define BRAHMINS_H

	#ifndef BRAHMINS
		#ifdef __SERVER
			import void brahminInitSearch( Critter& cr ) from "brahmins";
			import bool BrahminPetting( Critter& cr, Item& item, Critter@ targetCr ) from "brahmins";
			import bool CheckBrahminPregnancy( Critter& cr, Critter& target ) from "brahmins";
		#endif // __SERVER
	#endif // BRAHMINS


#endif // BRAHMINS_H