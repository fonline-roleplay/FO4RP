#include "binyan_h.fos"

import void ReturnAny( any& a ) from "binyan";

void GetAnyValue( Critter& cr, int, int, int )
{
    int    num;
    binyan b2;
    any    c;
    ReturnAny( c );
    c.retrieve( b2 );
    Log( "Value retrieved is " + b2.value );
}
